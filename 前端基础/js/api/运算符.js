/* ----------------------------------------- 隐式转换  ----------------------------------------- */
;(function () {
  console.log({} + []) // 浏览器中 0 会把 {} 当作一个代码块, 后边当成一个整体
  console.log((!+[] + [] + ![]).length) // 9 => true + '' + false
})()
